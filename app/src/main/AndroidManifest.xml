<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".zhanghaoshezhiActivity"></activity>
        <activity android:name=".index_guanliyuanActivity" />
        <activity android:name=".index_weixiurenyuanActivity" />
        <activity android:name=".index_xueshengActivity" />
        <activity
            android:name=".loginActivity"
            android:theme="@style/login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingActivity"
            android:label="@string/setting" />
        <activity
            android:name=".index_jiaoshiActivity"
            android:label="@string/jiaoshi_login" />
        <activity
            android:name=".tongzhiActivity"
            android:label="@string/tongzhi" />
        <activity
            android:name=".baoxiumesaageActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>